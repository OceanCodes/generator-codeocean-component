import React, {
  PureComponent,
} from 'react';

export type Props = {};

type State = {};

export default class <%= componentName %> extends PureComponent <Props, State> {
  static defaultProps = {};

  render() {
    return (
      <div />
    );
  }
}
