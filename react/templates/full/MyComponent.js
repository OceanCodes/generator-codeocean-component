import React, {
  Component,
} from 'react';

export type Props = {};

type State = {};

export default class <%= componentName %> extends Component <Props, State> {
  static defaultProps = {};

  props: Props;

  render() {
    return (
      <div />
    );
  }
}
